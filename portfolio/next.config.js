const config = {
        reactStrictMode         : true,
        onDemandEntries         : {
                maxInactiveAge          : 60 * 1000,
                pagesBufferLength       : 5,
        }
};

module.exports = config
